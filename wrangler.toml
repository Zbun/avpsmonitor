name = "avpsmonitor"
main = "worker/index.js"
compatibility_date = "2024-12-01"

# 静态资源配置
[assets]
directory = "./dist"

# KV 命名空间绑定
[[kv_namespaces]]
binding = "VPS_KV"
id = "YOUR_KV_ID"  # 部署后在 Dashboard 中配置

# 环境变量（在 Dashboard 中配置，这里只是占位）
# VPS_AUTH_TOKEN = "your-token"

