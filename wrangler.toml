name = "avpsmonitor"
main = "worker/index.js"
compatibility_date = "2024-12-01"

# 静态资源配置
[assets]
directory = "./dist"

# KV 绑定在 Cloudflare Dashboard 中配置，不在这里配置
# 部署后，在 Settings → Variables → KV Namespace Bindings 中添加：
# 变量名: VPS_KV
# 命名空间: 选择你创建的 KV
