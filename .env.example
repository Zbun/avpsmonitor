# ====== 前端配置 ======

# 是否使用真实 API（true 使用后端 API，false 使用 mock 数据展示）
VITE_USE_REAL_API=true

# API 服务器地址（留空表示使用相对路径，适配 Vercel 部署）
# 本地开发时可设置为 http://localhost:3000
VITE_API_URL=


# ====== Vercel 环境变量（在 Vercel Dashboard 配置）======

# API 认证 Token（必需）- Agent 上报数据时需要此 Token
# API_TOKEN=your-secret-token-here

# Vercel KV 会自动注入以下变量，无需手动配置：
# KV_URL, KV_REST_API_URL, KV_REST_API_TOKEN, KV_REST_API_READ_ONLY_TOKEN
